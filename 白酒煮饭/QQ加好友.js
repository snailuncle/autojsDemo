 textcontains(＂添加好友＂).waitfor()
 console.show()
 if (textcontains(x) exists() t
 if (text contains(＂问题＂).exists())
 {
   click(＂取消＂)
   relset
   var c = textcontains＂＂).().text() var a = c split(＂
   num = a[2].split(＂岁＂)
   num = (num[])
   1 og()
   Log(a)
   log()
   if (num＞ = 20 && num＜ = 23) {
     click(＂发送＂)
     reset
     click(＂取消＂)
   }
   reiser
   log(＂没有＂)
   click(＂取消＂)
 }